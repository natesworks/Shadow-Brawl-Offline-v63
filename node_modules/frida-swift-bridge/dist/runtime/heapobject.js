/* Defined in stdlib/public/SwiftShims/HeapObject.h */
export class HeapObject {
    handle;
    static SIZEOF = Process.pointerSize * 2;
    metadata;
    refCounts;
    constructor(handle) {
        this.handle = handle;
    }
    getMetadata(c) {
        return new c(this.handle.readPointer());
    }
}
export class BoxPair {
    object;
    buffer;
    constructor(objAndBuffer) {
        this.object = new HeapObject(objAndBuffer[0]);
        this.buffer = objAndBuffer[1];
    }
}
